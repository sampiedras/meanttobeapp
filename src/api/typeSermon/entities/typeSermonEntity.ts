export interface TypeSermonEntity {
  id: number;
  name: string;
}

export interface ITypeSermon {
  name: string;
  pk: string;
  sk: string;
}

export interface ITypeSermonResponse {
  id: string;
  name: string;
}
