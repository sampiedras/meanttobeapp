export type TagEntity = {
  id: string;
  description: string;
  name: string;
  img: string;
};

export type TagResponseEntity = {
  pk: string;
  sk: string;
  description: string;
  name: string;
  img: string;
};
